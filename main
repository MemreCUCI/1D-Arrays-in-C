#include<stdio.h>

int main()
{
int sum = 0;
int n,i,j;
scanf("%d",&n);
int arr[n];

for(i=0;i<n;i++)
{
    scanf("%d",&arr[i]);

}

for(j=0;j<n;j++)
{
    sum += arr[j];
}

printf("%d",sum);

    return 0;
}
